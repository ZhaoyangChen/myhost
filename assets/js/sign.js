$(window).on("load", function() {	switchPanel();	window.onhashchange = function(){		switchPanel();	}	$('#sign-up-form').ajaxForm(		{			data: {action : 'signup'},			success: function(msg) {				msg = $.parseJSON(msg);				if (msg.status === 'success') {					resPage('注册成功, 测试阶段你可以再帮我试试登录功能>_<')				} else {					resPage('注册失败, 原因很多啦, 等我有空帮你查')				}			}		}	);	$('#sign-in-form').ajaxForm(		{			data: {action : 'signin'},			success: function(msg) {				msg = $.parseJSON(msg);				if (msg.status === 'success') {					resPage('登录成功, 测试阶段没有下文了')				} else {					resPage('登录失败, 难道是我写出了BUG')				}			}		}	);});function switchPanel() {	if (window.location.hash == '#up') {		$('#signup-inputs').show();		$('#signin-inputs').hide();	} else {		$('#signin-inputs').show();		$('#signup-inputs').hide();	}}function resPage(info) {	var mask = $('.info-mask');	var mask_info = $('#res-info');	mask.show();	mask_info.text(info);}